const translations = {
  es: {
    'nav.quienes': '¿Quiénes Somos?',
    'nav.servicios': 'Servicios',
    'nav.equipo': 'Equipo',
    'nav.contacto': 'Contáctenos',
    'hero.title': 'Transforma tus datos en decisiones estratégicas',
    'hero.subtitle': 'En Datela BI ayudamos a las empresas a convertir datos en decisiones inteligentes.',
    'hero.stat2-label': 'Años de experiencia',
    'about.label': 'La empresa',
    'about.title': '¿Quiénes Somos?',
    'about.desc': 'Somos un grupo de expertos en analítica, tecnologías, procesos y asesores en naturaleza de negocios que nos permite brindarle servicios para capitalizar eficiente y eficazmente sus datos. Convertiremos sus datos en su principal activo para tomas oportunas de decisiones y desarrollo de planes estratégicos encaminados a optimizar costos, maximizar ventas y utilidades, fidelizar clientes e incrementar la participación en el mercado.',
    'acc.title1': 'Propósito',
    'acc.body1': 'Impulsar organizaciones más inteligentes mediante el uso estratégico de los datos, transformando la información en conocimiento que genere mejores decisiones, eficiencia y crecimiento sostenible.',
    'acc.title2': 'Misión',
    'acc.body2': 'En Datela BI desarrollamos soluciones de inteligencia de negocios, analítica empresarial, inteligencia artificial y gestión de datos, que permiten a las organizaciones transformar grandes volúmenes de información en conocimiento estratégico, optimizando la toma de decisiones, mejorando el desempeño empresarial y generando valor a través de la innovación tecnológica.',
    'acc.title3': 'Visión',
    'acc.body3': 'Para el año 2030, Datela BI será reconocida como una empresa líder en América Latina en soluciones de analítica avanzada, inteligencia de negocios e inteligencia artificial, impulsando la transformación digital de las organizaciones mediante el uso estratégico de los datos y el desarrollo de soluciones tecnológicas innovadoras.',
    'acc.title4': 'Valores',
    'acc.body4': '<ul><li><strong>• Innovación:</strong> Promovemos el uso de nuevas tecnologías y metodologías para generar soluciones analíticas de alto impacto.</li><li><strong>• Integridad:</strong> Actuamos con ética, transparencia y responsabilidad en el manejo de la información y en nuestras relaciones comerciales.</li><li><strong>• Orientación al cliente:</strong> Comprendemos las necesidades de cada organización para ofrecer soluciones analíticas que generen valor real.</li><li><strong>• Excelencia:</strong> Buscamos altos estándares de calidad en cada proyecto, servicio y solución tecnológica que desarrollamos.</li><li><strong>• Aprendizaje continuo:</strong> Fomentamos la actualización permanente en tecnologías de datos, analítica e inteligencia artificial.</li><li><strong>• Colaboración:</strong> Promovemos el trabajo en equipo y la cooperación con clientes y aliados estratégicos.</li></ul>',
    'acc.title5': 'Propuesta de valor',
    'acc.body5': 'En <strong>Datela BI</strong> ayudamos a las organizaciones a convertir datos en decisiones inteligentes, integrando herramientas de inteligencia de negocios, analítica avanzada e inteligencia artificial para mejorar la eficiencia operativa, identificar oportunidades de negocio y fortalecer la competitividad empresarial.',
    'acc.title6': 'Enfoque estratégico',
    'acc.body6': '<ul><li><strong>• Inteligencia de Negocios (Business Intelligence):</strong> Diseño e implementación de plataformas de BI para la visualización y análisis de información empresarial mediante tableros de control e indicadores estratégicos.</li><li><strong>• Analítica de datos y analítica avanzada:</strong> Análisis descriptivo, predictivo y prescriptivo para apoyar la toma de decisiones basadas en datos.</li><li><strong>• Inteligencia Artificial aplicada a negocios:</strong> Desarrollo de modelos de aprendizaje automático y automatización de procesos analíticos para mejorar la eficiencia empresarial.</li><li><strong>• Ingeniería y gestión de datos:</strong> Integración, procesamiento y gestión de grandes volúmenes de datos provenientes de múltiples fuentes.</li><li><strong>• Desarrollo de software analítico:</strong> Diseño y desarrollo de soluciones tecnológicas personalizadas para la explotación y análisis de datos.</li><li><strong>• Consultoría y asesoría en analítica empresarial:</strong> Acompañamiento estratégico a empresas en procesos de transformación digital y cultura de datos.</li></ul>',
    'services.label': 'Lo que hacemos',
    'services.title': 'Servicios',
    'services.service1_title': 'Inteligencia de Negocios (BI)',
    'services.service2_title': 'Analítica Avanzada & IA',
    'services.service3_title': 'Ingeniería de Datos',
    'services.service4_title': 'Desarrollo de Software Analítico',
    'services.service5_title': 'Consultoría Estratégica',
    'services.service6_title': 'Capacitación & Workshops',
    'team.label': 'El equipo',
    'team.title': 'Conoce a nuestro equipo',
    'team.ceo_name': 'Juan David Vargas',
    'team.ceo_role': 'CEO & Fundador',
    'team.ceo_bio': 'Juan David es un líder visionario con más de 20 años de experiencia en el sector de la tecnología y la analítica de datos. Su pasión por transformar datos en estrategias de negocio lo llevó a fundar Datela BI, donde impulsa la innovación y el crecimiento. Es experto en Business Intelligence, Machine Learning y Big Data, y un firme creyente en el poder de los datos para impulsar el éxito empresarial.',
    'contact.label': 'Hablemos',
    'contact.title': 'Contáctenos',
    'contact.name_label': 'Nombre Completo',
    'contact.name_placeholder': 'Tu nombre',
    'contact.email_label': 'Correo Electrónico',
    'contact.email_placeholder': 'tu@correo.com',
    'contact.subject_label': 'Asunto',
    'contact.subject_placeholder': '¿En qué podemos ayudarte?',
    'contact.message_label': 'Mensaje',
    'contact.message_placeholder': 'Cuéntanos sobre tu proyecto...', 
    'contact.submit_button': 'Enviar mensaje',
    'contact.privacy_policy': 'Política de privacidad',
    'contact.terms_of_service': 'Términos de servicio',
    'footer.copyright': '&copy; <span id="year"></span> Datela BI S.A.S. Todos los derechos reservados.',
    'footer.company_title': 'Empresa',
    'footer.company_about': '¿Quiénes Somos?',
    'footer.company_team': 'Equipo',
    'footer.company_partners': 'Aliados',
    'footer.services_title': 'Servicios',
    'footer.service1': 'Servicio 1',
    'footer.service2': 'Servicio 2',
    'footer.service3': 'Servicio 3',
    'footer.contact_title': 'Contacto',
    'footer.contact_phone': '[Teléfono]',
    'footer.contact_email': '[Correo]',
    'footer.privacy_policy': 'Política de privacidad',
    'footer.terms_of_use': 'Términos de uso',
    'privacy_modal.title': 'Política de Tratamiento de Datos Personales',
    'privacy_modal.responsible': '<strong>Responsable:</strong> DATELA BI S.A.S. | NIT: 802018415-1',
    'privacy_modal.purpose': 'Sus datos serán utilizados para: Mantenimiento de relación comercial, facturación y envío de información de servicios...', 
    'privacy_modal.contact': 'Para consultas o reclamos: analitica@datelabi.com'
  },
  en: {
    'nav.quienes': 'About Us',
    'nav.servicios': 'Services',
    'nav.equipo': 'Team',
    'nav.contacto': 'Contact Us',
    'hero.title': 'Transform your data into strategic decisions',
    'hero.subtitle': 'At Datela BI we help companies turn data into intelligent decisions.',
    'hero.stat2-label': 'Years of experience',
    'about.label': 'The Company',
    'about.title': 'About Us',
    'about.desc': 'We are a group of experts in analytics, technologies, processes, and business advisors that allows us to provide services to efficiently and effectively capitalize on your data. We will turn your data into your main asset for timely decision-making and the development of strategic plans aimed at optimizing costs, maximizing sales and profits, retaining customers, and increasing market share.',
    'acc.title1': 'Purpose',
    'acc.body1': 'To drive smarter organizations through the strategic use of data, transforming information into knowledge that generates better decisions, efficiency, and sustainable growth.',
    'acc.title2': 'Mission',
    'acc.body2': 'At Datela BI, we develop business intelligence, business analytics, artificial intelligence, and data management solutions that enable organizations to transform large volumes of information into strategic knowledge, optimizing decision-making, improving business performance, and generating value through technological innovation.',
    'acc.title3': 'Vision',
    'acc.body3': 'By 2030, Datela BI will be recognized as a leading company in Latin America in advanced analytics, business intelligence, and artificial intelligence solutions, driving the digital transformation of organizations through the strategic use of data and the development of innovative technological solutions.',
    'acc.title4': 'Values',
    'acc.body4': '<ul><li><strong>• Innovation:</strong> We promote the use of new technologies and methodologies to generate high-impact analytical solutions.</li><li><strong>• Integrity:</strong> We act with ethics, transparency, and responsibility in information management and our business relationships.</li><li><strong>• Client Orientation:</strong> We understand the needs of each organization to offer analytical solutions that generate real value.</li><li><strong>• Excellence:</strong> We seek high quality standards in every project, service, and technological solution we develop.</li><li><strong>• Continuous Learning:</strong> We foster permanent updating in data technologies, analytics, and artificial intelligence.</li><li><strong>• Collaboration:</strong> We promote teamwork and cooperation with clients and strategic partners.</li></ul>',
    'acc.title5': 'Value Proposition',
    'acc.body5': 'At <strong>Datela BI</strong>, we help organizations turn data into intelligent decisions, integrating business intelligence tools, advanced analytics, and artificial intelligence to improve operational efficiency, identify business opportunities, and strengthen business competitiveness.',
    'acc.title6': 'Strategic Approach',
    'acc.body6': '<ul><li><strong>• Business Intelligence:</strong> Design and implementation of BI platforms for visualizing and analyzing business information through dashboards and strategic indicators.</li><li><strong>• Data Analytics and Advanced Analytics:</strong> Descriptive, predictive, and prescriptive analysis to support data-driven decision-making.</li><li><strong>• Artificial Intelligence applied to business:</strong> Development of machine learning models and automation of analytical processes to improve business efficiency.</li><li><strong>• Data Engineering and Management:</strong> Integration, processing, and management of large volumes of data from multiple sources.</li><li><strong>• Analytical Software Development:</strong> Design and development of customized technological solutions for data exploitation and analysis.</li><li><strong>• Business Analytics Consulting and Advisory:</strong> Strategic accompaniment to companies in digital transformation processes and data culture.</li></ul>',
    'services.label': 'What we do',
    'services.title': 'Services',
    'services.service1_title': 'Business Intelligence (BI)',
    'services.service2_title': 'Advanced Analytics & AI',
    'services.service3_title': 'Data Engineering',
    'services.service4_title': 'Analytical Software Development',
    'services.service5_title': 'Strategic Consulting',
    'services.service6_title': 'Training & Workshops',
    'team.label': 'The Team',
    'team.title': 'Meet Our Team',
    'team.ceo_name': 'Juan David Vargas',
    'team.ceo_role': 'CEO & Founder',
    'team.ceo_bio': 'Juan David is a visionary leader with over 20 years of experience in the technology and data analytics sector. His passion for transforming data into business strategies led him to found Datela BI, where he drives innovation and growth. He is an expert in Business Intelligence, Machine Learning, and Big Data, and a firm believer in the power of data to drive business success.',
    'contact.label': 'Let's Talk',
    'contact.title': 'Contact Us',
    'contact.name_label': 'Full Name',
    'contact.name_placeholder': 'Your name',
    'contact.email_label': 'Email Address',
    'contact.email_placeholder': 'your@email.com',
    'contact.subject_label': 'Subject',
    'contact.subject_placeholder': 'How can we help you?',
    'contact.message_label': 'Message',
    'contact.message_placeholder': 'Tell us about your project...', 
    'contact.submit_button': 'Send Message',
    'contact.privacy_policy': 'Privacy Policy',
    'contact.terms_of_service': 'Terms of Service',
    'footer.copyright': '&copy; <span id="year"></span> Datela BI S.A.S. All rights reserved.',
    'footer.company_title': 'Company',
    'footer.company_about': 'About Us',
    'footer.company_team': 'Team',
    'footer.company_partners': 'Partners',
    'footer.services_title': 'Services',
    'footer.service1': 'Service 1',
    'footer.service2': 'Service 2',
    'footer.service3': 'Service 3',
    'footer.contact_title': 'Contact',
    'footer.contact_phone': '[Phone]',
    'footer.contact_email': '[Email]',
    'footer.privacy_policy': 'Privacy Policy',
    'footer.terms_of_use': 'Terms of Use',
    'privacy_modal.title': 'Personal Data Processing Policy',
    'privacy_modal.responsible': '<strong>Responsible:</strong> DATELA BI S.A.S. | NIT: 802018415-1',
    'privacy_modal.purpose': 'Your data will be used for: Maintenance of commercial relationship, billing and sending of service information...', 
    'privacy_modal.contact': 'For inquiries or claims: analitica@datelabi.com'
  }
};
/* ============================================================
   1. UTILIDADES
============================================================ */

/**
 * Selector abreviado
 * @param {string} selector
 * @param {Element|Document} [ctx=document]
 * @returns {Element|null}
 */
const $ = (selector, ctx = document) => ctx.querySelector(selector);

/**
 * Selector múltiple abreviado
 * @param {string} selector
 * @param {Element|Document} [ctx=document]
 * @returns {NodeList}
 */
const $$ = (selector, ctx = document) => ctx.querySelectorAll(selector);

/**
 * Ejecuta un callback cuando el DOM está listo
 * @param {Function} fn
 */
const onReady = (fn) => {
  if (document.readyState !== 'loading') fn();
  else document.addEventListener('DOMContentLoaded', fn);
};

/* ============================================================
   2. NAVBAR — Scroll + Hamburger
============================================================ */

const initNavbar = () => {
  const navbar    = $('#navbar');
  const hamburger = $('#hamburger');
  const navLinks  = $('#navLinks');

  if (!navbar) return;

  // Scroll: agrega clase "scrolled" al bajar
  const handleScroll = () => {
    if (window.scrollY > 40) {
      navbar.classList.add('scrolled');
    } else {
      navbar.classList.remove('scrolled');
    }
  };

  window.addEventListener('scroll', handleScroll, { passive: true });
  handleScroll(); // estado inicial

  // Hamburger toggle
  if (hamburger && navLinks) {
    hamburger.addEventListener('click', () => {
      const isOpen = hamburger.classList.toggle('open');
      navLinks.classList.toggle('open', isOpen);
      hamburger.setAttribute('aria-expanded', String(isOpen));
      // Prevenir scroll del body cuando el menú está abierto
      document.body.style.overflow = isOpen ? 'hidden' : '';
    });

    // Cerrar menú al hacer clic en un enlace
    $$('.nav-link', navLinks).forEach((link) => {
      link.addEventListener('click', () => {
        hamburger.classList.remove('open');
        navLinks.classList.remove('open');
        hamburger.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
      });
    });

    // Cerrar menú al hacer clic fuera
    document.addEventListener('click', (e) => {
      if (
        navLinks.classList.contains('open') &&
        !navbar.contains(e.target)
      ) {
        hamburger.classList.remove('open');
        navLinks.classList.remove('open');
        hamburger.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
      }
    });
  }
};

/* ============================================================
   3. ACORDEÓN — Quiénes Somos
============================================================ */

const initAccordion = () => {
  const accordion = $('#accordion');
  if (!accordion) return;

  const items = $$('.accordion__item', accordion);

  items.forEach((item) => {
    const trigger = $('.accordion__trigger', item);
    const panel   = $('.accordion__panel', item);

    if (!trigger || !panel) return;

    trigger.addEventListener('click', () => {
      const isOpen = item.classList.contains('is-open');

      // Cerrar todos los demás (comportamiento de acordeón exclusivo)
      items.forEach((otherItem) => {
        if (otherItem !== item) {
          closeAccordionItem(otherItem);
        }
      });

      // Toggle el actual
      if (isOpen) {
        closeAccordionItem(item);
      } else {
        openAccordionItem(item);
      }
    });

    // Soporte de teclado
    trigger.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        trigger.click();
      }
    });
  });
};

/**
 * Abre un ítem del acordeón con animación
 * @param {Element} item
 */
const openAccordionItem = (item) => {
  const trigger = $('.accordion__trigger', item);
  const panel   = $('.accordion__panel', item);
  const body    = $('.accordion__body', item);

  if (!trigger || !panel || !body) return;

  item.classList.add('is-open');
  trigger.setAttribute('aria-expanded', 'true');
  panel.removeAttribute('hidden');

  // Animación de altura
  const targetHeight = body.scrollHeight + 'px';
  panel.style.maxHeight = '0';
  // Forzar reflow
  panel.getBoundingClientRect();
  panel.style.maxHeight = targetHeight;
};

/**
 * Cierra un ítem del acordeón con animación
 * @param {Element} item
 */
const closeAccordionItem = (item) => {
  const trigger = $('.accordion__trigger', item);
  const panel   = $('.accordion__panel', item);

  if (!trigger || !panel) return;

  item.classList.remove('is-open');
  trigger.setAttribute('aria-expanded', 'false');
  panel.style.maxHeight = '0';

  // Ocultar después de la transición
  panel.addEventListener(
    'transitionend',
    () => {
      if (!item.classList.contains('is-open')) {
        panel.setAttribute('hidden', '');
        panel.style.maxHeight = '';
      }
    },
    { once: true }
  );
};

/* ============================================================
   4. SCROLL ANIMATIONS — Intersection Observer
============================================================ */

const initScrollAnimations = () => {
  const elements = $$('.animate-on-scroll');
  if (!elements.length) return;

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    },
    {
      threshold: 0.12,
      rootMargin: '0px 0px -40px 0px',
    }
  );

  elements.forEach((el) => observer.observe(el));
};

/* ============================================================
   5. BACK TO TOP
============================================================ */

const initBackToTop = () => {
  const btn = $('#backToTop');
  if (!btn) return;

  window.addEventListener(
    'scroll',
    () => {
      if (window.scrollY > 400) {
        btn.classList.add('visible');
      } else {
        btn.classList.remove('visible');
      }
    },
    { passive: true }
  );

  btn.addEventListener('click', () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
};

/* ============================================================
   6. SELECTOR DE IDIOMA
============================================================ */

const initLangSwitcher = () => {
  const switcher = $('#langSwitcher');
  if (!switcher) return;

  const buttons = $$('.lang-btn', switcher);

  buttons.forEach((btn) => {
    btn.addEventListener('click', () => {
      // 1. Cambiar estado visual de los botones
      buttons.forEach((b) => {
        b.classList.remove('active');
        b.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('active');
      btn.setAttribute('aria-pressed', 'true');

      // 2. Obtener el idioma seleccionado
      const lang = btn.dataset.lang;
      document.documentElement.setAttribute('lang', lang);
      localStorage.setItem('selectedLanguage', lang);

      // 3. Traducir los textos
      document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (translations[lang] && translations[lang][key]) {
          if (el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') {
            el.placeholder = translations[lang][key];
          } else if (key.includes('body') || key.includes('copyright') || key.includes('responsible') || key.includes('purpose')) {
            el.innerHTML = translations[lang][key]; 
          } else {
            el.textContent = translations[lang][key];
          }
        }
      });

      console.info(`[Datela BI] Idioma cambiado a: ${lang.toUpperCase()}`);
    });
  });

  // 4. Cargar el idioma guardado al abrir la página
  const savedLang = localStorage.getItem('selectedLanguage') || 'es';
  const targetBtn = document.querySelector(`.lang-btn[data-lang="${savedLang}"]`);
  if (targetBtn) targetBtn.click();
};

/* ============================================================
   7. FORMULARIO DE CONTACTO CON RECAPTCHA AVANZADO
============================================================ */

const initContactForm = () => {
  const form       = $('#contactForm');
  const notice     = $('#formNotice');
  const submitBtn  = $('#submitBtn');

  if (!form || !notice || !submitBtn) return;

  // Validadores de campo
  const validators = {
    name: (value) => {
      if (!value.trim()) return 'El nombre es requerido';
      if (value.trim().length < 3) return 'El nombre debe tener al menos 3 caracteres';
      if (!/^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$/.test(value)) return 'El nombre solo puede contener letras';
      return null;
    },
    email: (value) => {
      if (!value.trim()) return 'El correo es requerido';
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(value)) return 'Por favor, ingresa un correo válido';
      return null;
    },
    subject: (value) => {
      if (!value.trim()) return 'El asunto es requerido';
      if (value.trim().length < 5) return 'El asunto debe tener al menos 5 caracteres';
      if (value.trim().length > 100) return 'El asunto no puede exceder 100 caracteres';
      return null;
    },
    message: (value) => {
      if (!value.trim()) return 'El mensaje es requerido';
      if (value.trim().length < 10) return 'El mensaje debe tener al menos 10 caracteres';
      if (value.trim().length > 2000) return 'El mensaje no puede exceder 2000 caracteres';
      return null;
    }
  };

  // Validar campo individual
  const validateField = (fieldName) => {
    const field = $(`#${fieldName}`, form);
    const errorEl = $(`#${fieldName}Error`, form);
    if (!field || !errorEl) return true;

    const error = validators[fieldName]?.(field.value);
    
    if (error) {
      field.classList.add('field--error');
      errorEl.textContent = error;
      return false;
    } else {
      field.classList.remove('field--error');
      errorEl.textContent = '';
      return true;
    }
  };

  // Validar reCAPTCHA
  const validateRecaptcha = () => {
    const recaptchaError = $('#recaptchaError', form);
    const recaptchaResponse = typeof grecaptcha !== 'undefined' ? grecaptcha.getResponse() : null;
    
    if (!recaptchaResponse) {
      if (recaptchaError) recaptchaError.textContent = 'Por favor, completa el reCAPTCHA';
      return false;
    } else {
      if (recaptchaError) recaptchaError.textContent = '';
      return true;
    }
  };

  // Validación en tiempo real
  $$('input[required], textarea[required]', form).forEach((field) => {
    field.addEventListener('blur', () => {
      const fieldName = field.id;
      if (fieldName) validateField(fieldName);
    });

    field.addEventListener('input', () => {
      const errorEl = $(`#${field.id}Error`, form);
      if (errorEl && errorEl.textContent) {
        validateField(field.id);
      }
    });
  });

  // Submit
  form.addEventListener('submit', async (e) => {
    e.preventDefault();

    // Limpiar errores previos
    $$('.form__error', form).forEach((el) => el.textContent = '');
    $$('.field--error', form).forEach((el) => el.classList.remove('field--error'));

    // Validar todos los campos
    const isNameValid = validateField('name');
    const isEmailValid = validateField('email');
    const isSubjectValid = validateField('subject');
    const isMessageValid = validateField('message');
    const isRecaptchaValid = validateRecaptcha();

    if (!isNameValid || !isEmailValid || !isSubjectValid || !isMessageValid || !isRecaptchaValid) {
      notice.textContent = 'Por favor, corrige los errores en el formulario.';
      notice.style.color = '#FF6B6B';
      return;
    }

    // Deshabilitar botón
    submitBtn.disabled = true;
    const originalHTML = submitBtn.innerHTML;
    submitBtn.innerHTML = '<i class="ph ph-spinner" aria-hidden="true" style="animation: spin 1s linear infinite;"></i> Enviando...';

    try {
      // Aquí iría la llamada a tu API backend
      // Ejemplo:
      // const response = await fetch('/api/contact', {
      //   method: 'POST',
      //   headers: { 'Content-Type': 'application/json' },
      //   body: JSON.stringify({
      //     name: $('#name', form).value,
      //     email: $('#email', form).value,
      //     subject: $('#subject', form).value,
      //     message: $('#message', form).value,
      //     recaptchaToken: grecaptcha.getResponse()
      //   })
      // });
      // if (!response.ok) throw new Error('Error en la respuesta del servidor');

      // Simulación de envío
      await new Promise((resolve) => setTimeout(resolve, 1500));

      notice.textContent = '✓ ¡Mensaje enviado exitosamente! Nos pondremos en contacto pronto.';
      notice.style.color = 'var(--color-cyan)';
      form.reset();
      
      if (typeof grecaptcha !== 'undefined') {
        grecaptcha.reset();
      }

      // Limpiar mensaje después de 6 segundos
      setTimeout(() => {
        notice.textContent = '';
      }, 6000);

    } catch (error) {
      console.error('Error al enviar formulario:', error);
      notice.textContent = '✗ Error al enviar el mensaje. Por favor, intenta de nuevo.';
      notice.style.color = '#FF6B6B';
    } finally {
      submitBtn.disabled = false;
      submitBtn.innerHTML = originalHTML;
    }
  });
};

/* ============================================================
   8. AÑO DINÁMICO EN FOOTER
============================================================ */

const initFooterYear = () => {
  const yearEl = $('#year');
  if (yearEl) {
    yearEl.textContent = new Date().getFullYear();
  }
};

/* ============================================================
   9. SMOOTH SCROLL PARA ANCLAS INTERNAS
============================================================ */

const initSmoothScroll = () => {
  $$('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener('click', (e) => {
      const targetId = anchor.getAttribute('href');
      if (targetId === '#') return;

      const target = $(targetId);
      if (!target) return;

      e.preventDefault();

      const navbarHeight = parseInt(
        getComputedStyle(document.documentElement)
          .getPropertyValue('--navbar-height'),
        10
      ) || 72;

      const top = target.getBoundingClientRect().top + window.scrollY - navbarHeight;

      window.scrollTo({ top, behavior: 'smooth' });
    });
  });
};

/* ============================================================
   10. ACTIVE NAV LINK — Highlight al hacer scroll
============================================================ */

const initActiveNavLink = () => {
  const sections = $$('section[id]');
  const navLinks = $$('.nav-link');

  if (!sections.length || !navLinks.length) return;

  const navbarHeight = parseInt(
    getComputedStyle(document.documentElement)
      .getPropertyValue('--navbar-height'),
    10
  ) || 72;

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const id = entry.target.getAttribute('id');
          navLinks.forEach((link) => {
            const href = link.getAttribute('href');
            link.classList.toggle('active', href === `#${id}`);
          });
        }
      });
    },
    {
      rootMargin: `-${navbarHeight}px 0px -60% 0px`,
      threshold: 0,
    }
  );

  sections.forEach((section) => observer.observe(section));
};

/* ============================================================
   11. ANIMACIÓN DE SPINNER
============================================================ */

const initSpinnerAnimation = () => {
  if (!document.querySelector('style[data-spinner]')) {
    const style = document.createElement('style');
    style.setAttribute('data-spinner', '');
    style.textContent = `
      @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
      }
    `;
    document.head.appendChild(style);
  }
};

/* ============================================================
   12. INICIALIZACIÓN PRINCIPAL
============================================================ */

onReady(() => {
  initNavbar();
  initAccordion();
  initScrollAnimations();
  initBackToTop();
  initLangSwitcher();
  initContactForm();
  initFooterYear();
  initSmoothScroll();
  initActiveNavLink();
  initSpinnerAnimation();

  console.info('%c Datela BI S.A.S. — Landing Page v2.0 (con reCAPTCHA) ', 'background:#0B2046;color:#00A4E4;font-weight:bold;padding:4px 8px;border-radius:4px;');
});
